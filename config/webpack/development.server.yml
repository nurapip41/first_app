# Note: You must restart bin/webpack-dev-server for changes to take effect

default: &default
  enabled: false
  host: localhost
  port: 8080

development:
  <<: *default

test:
  <<: *default
  enabled: false

production:
  <<: *default
  enabled: false
